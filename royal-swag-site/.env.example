# Copy to .env.local for development. Set the same keys in Vercel → Project → Settings → Environment Variables.

# --- Public (browser / edge-safe) ---
# Analytics (Meta Pixel Helper + GA4 DebugView)
NEXT_PUBLIC_META_PIXEL_ID=
NEXT_PUBLIC_GA4_ID=

# Admin dashboard (Supabase Auth)
ADMIN_ALLOWED_EMAILS=admin@eximburginternational.in
# Optional legacy fallback used by middleware/session helpers
NEXT_PUBLIC_ADMIN_EMAIL=
# Secret URL segment only — e.g. rs_eximburg_9x7k → https://yoursite.com/rs_eximburg_9x7k
# Never commit the real value; set in Vercel env for production.
ADMIN_SECRET_PATH=
# --- Contact (set real values in production — no placeholders) ---
NEXT_PUBLIC_WHATSAPP_NUMBER=917096553300
NEXT_PUBLIC_PHONE_DISPLAY=+91 70965 53300
NEXT_PUBLIC_PHONE_NUMBER=+91 70965 53300
NEXT_PUBLIC_EMAIL=hello@royalswag.in
NEXT_PUBLIC_SUPPORT_EMAIL=hello@royalswag.in

# --- Legal ---
NEXT_PUBLIC_FSSAI_LICENSE=
NEXT_PUBLIC_FSSAI_NUMBER=
NEXT_PUBLIC_COMPANY_ADDRESS=Eximburg International Pvt Ltd, Plot No. 150, 3rd Floor, Amrut Udhyognagar, Kholvad, Kamrej, Surat — Gujarat 394185
NEXT_PUBLIC_COMPANY_GSTIN=
NEXT_PUBLIC_ADDRESS_LINE1=Plot No. 150, 3rd Floor, Amrut Udhyognagar
NEXT_PUBLIC_ADDRESS_LINE2=Kholvad, Kamrej, Surat — Gujarat 394185

# --- Social ---
NEXT_PUBLIC_INSTAGRAM_URL=https://www.instagram.com/royalswag.in/
NEXT_PUBLIC_YOUTUBE_URL=https://www.youtube.com/@royalswag
NEXT_PUBLIC_FACEBOOK_URL=https://www.facebook.com/royalswag

NEXT_PUBLIC_RAZORPAY_KEY_ID=
NEXT_PUBLIC_SITE_URL=https://lungdetox.royalswag.in

# Product UI (optional overrides; code has defaults)
# Primary checkout price — change only with Manoj Bhai approval
NEXT_PUBLIC_PRODUCT_PRICE=699
NEXT_PUBLIC_PRODUCT_MRP=999
NEXT_PUBLIC_STOCK_COUNT=38
NEXT_PUBLIC_ORDER_COUNT=12
NEXT_PUBLIC_STICKY_BAR_PRICE=699
# 48h offer countdown seed (Unix ms) — resets on fixed 48h boundaries from this anchor
NEXT_PUBLIC_TIMER_SEED_TIMESTAMP=1704067200000
NEXT_PUBLIC_BUNDLE_3_PRICE=1799
NEXT_PUBLIC_BUNDLE_3_MRP=2997
NEXT_PUBLIC_SUBSCRIPTION_PRICE=649
NEXT_PUBLIC_PRICE_20DAY=349
NEXT_PUBLIC_PRICE_40DAY=599
NEXT_PUBLIC_PRICE_60DAY=849
NEXT_PUBLIC_MRP_20DAY=499
NEXT_PUBLIC_MRP_40DAY=698
NEXT_PUBLIC_MRP_60DAY=1047

# Supabase (client + save-lead API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- Server-only (never NEXT_PUBLIC_) ---
# Razorpay: key is returned from /api/create-order for checkout; keep secret on server.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=
# Use Razorpay test keys in .env.local for development (dashboard → API Keys → Test mode)

# Order notifications (server-only)
RESEND_API_KEY=
RESEND_FROM_EMAIL=orders@royalswag.in
RESEND_CC_EMAIL=hello@royalswag.in
MSG91_AUTH_KEY=
MSG91_TEMPLATE_ID=
MSG91_FLOW_ID=
MSG91_SENDER_ID=

# Supabase service role (save-lead / server writes)
SUPABASE_SERVICE_ROLE_KEY=

# PostgreSQL (Prisma: orders, lung tests, admin sessions)
# Copy into .env.local for Next.js + local Prisma (npm run db:*).
#
# For migrations against PRODUCTION (Vercel) DB only: create .env.production.local in repo root with ONE line:
#   DATABASE_URL="postgresql://..."   (paste exact URI from Vercel)
# Then: npm run db:migrate:prod
# If you see P3005 on migrate deploy, run once: npm run db:baseline-prod  (uses .env.production.local)
# Supabase direct (dev): postgresql://postgres:PASSWORD@db.REF.supabase.co:5432/postgres?sslmode=require
# Vercel/serverless: prefer Session pooler URI from Supabase Connect tab; often append &connection_limit=1 (see Prisma + Supabase docs).
# Neon: postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=

# Admin dashboard (/admin/login → cookie session in DB)
# ADMIN_PASSWORD_HASH = SHA-256 hex of your password (npm run admin:hash-password -- "yourpassword")
# Optional: if hash/env typos on Vercel break login, set ADMIN_PASSWORD (plain) — prefer hash only long-term.
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=
# Comma-separated allowed passwords (optional alternative to hash).
ADMIN_PASSWORD=
# Required for admin cookie sessions (HMAC-signed; no Postgres needed for login).
ADMIN_SECRET_KEY=

# Dashboard (/dashboard) — simple password gate (server-only)
DASHBOARD_PASSWORD=

# Google Sheets (service account — append rows to spreadsheet; share sheet with client_email)
GOOGLE_PROJECT_ID=
GOOGLE_PRIVATE_KEY_ID=
GOOGLE_PRIVATE_KEY=
GOOGLE_CLIENT_EMAIL=
GOOGLE_CLIENT_ID=
GOOGLE_SPREADSHEET_ID=
